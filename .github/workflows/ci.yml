name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  # ── Cross-platform test matrix ──────────────────────────────────────
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-latest, windows-latest]

    env:
      PYTHONPATH: "."

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --frozen

      # ── Architecture compliance gates (Linux only — requires grep) ──
      - name: Compliance gate — no `mcp` imports under engine/ or shared/
        if: runner.os == 'Linux'
        run: |
          if grep -rE "^(from|import)\s+mcp" engine/ shared/; then
            echo "::error::engine/** and shared/** must not import from mcp.*"
            exit 1
          fi

      - name: Compliance gate — no stdout writes from engine/
        if: runner.os == 'Linux'
        run: |
          if grep -rnE "^\s*print\(" engine/ \
              | grep -v "engine/output/display.py" \
              | grep -v "file=sys.stderr"; then
            echo "::error::stray print() in engine/** (must use display.py / stderr)"
            exit 1
          fi

      # ── Linting & static analysis ──
      - name: Lint (ruff)
        run: uv run ruff check .

      - name: Format check (ruff)
        run: uv run ruff format --check .

      - name: Type check (pyright)
        run: uv run pyright engine/ shared/ server.py

      - name: Verify tool docstrings
        run: uv run python tests/verify_tool_docstrings.py

      # ── Tests ──
      - name: Run tests
        run: uv run pytest tests/unit -q --tb=short

  # ── Constrained mode (8 GB VRAM target) ─────────────────────────────
  constrained:
    name: Constrained mode (ubuntu)
    runs-on: ubuntu-22.04

    env:
      MCP_CONSTRAINED_MODE: "1"
      PYTHONPATH: "."

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run tests in constrained mode
        run: uv run python -m pytest tests/unit -q --tb=short
